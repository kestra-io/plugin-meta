id: facebook_list_posts_test
namespace: io.kestra.tests

tasks:
  - id: list_posts
    type: io.kestra.plugin.meta.facebook.posts.List
    pageId: "123456789"
    accessToken: "test_access_token"
    limit: 10
    fetchType: FETCH
    apiBaseUrl: "{{ inputs.url }}"
